// ignore_for_file: prefer_const_constructors
import 'package:so_utils/so_utils.dart';
import 'package:test/test.dart';

void main() {
  group('SoUtils', () {
    test('can be instantiated', () {
      expect(SoUtils(), isNotNull);
    });
  });
}
